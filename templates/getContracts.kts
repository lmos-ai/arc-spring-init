function(
    name = "getContracts",
    description = "Retrieve Contract information of USer",
    params = types(
        string(
            name = "userId",
            description = "UserId to be used to fetch bill"
        )
    ),
) { (userId) ->

    }