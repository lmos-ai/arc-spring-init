function(
    name = "getTroubleTickets",
    description = "Retrieve TroubleTickets information of a user",
    params = types(
        string(
            name = "userId",
            description = "UserId to be used to fetch bill"
        )
    ),
) { (userId) ->

    }